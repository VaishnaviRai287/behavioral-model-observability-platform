# torch's default PyPI wheel pulls in ~10GB of NVIDIA CUDA packages even on a
# CPU-only host — this server only ever runs CPU inference, so pull the CPU-only
# build instead. PyPI stays primary (--extra-index-url) so every other package
# still resolves from there first; only torch needs the extra index.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.137.0
uvicorn[standard]==0.49.0
sqlalchemy==2.0.50
alembic==1.18.4
psycopg2-binary==2.9.12
pydantic==2.13.4
pydantic-settings==2.14.1
python-multipart==0.0.32
scikit-learn==1.7.2
scipy==1.15.3
torch==2.13.0
# Pinned below 2.0 because tensorflow<2.17 requires numpy<2.0 on Python <=3.11 —
# scikit-learn/scipy/faiss-cpu above are all satisfied by 1.26.x too.
numpy==1.26.4
faiss-cpu==1.14.3
tensorflow>=2.15,<2.17
celery==5.6.3
redis==8.0.0
